{
  "name": "todos",
  "version": "0.0.1",
  "private": true,
  "homepage": "/alena.sabaleuskaya.static.website",
  "devDependencies": {
    "aws-sdk": "^2.562.0",
    "dotenv": "^8.2.0",
    "fs-extra": "^8.1.0",
    "react-scripts": "^3.0.0"
  },
  "dependencies": {
    "prop-types": "^15.7.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-redux": "^7.0.2",
    "redux": "^4.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "test": "react-scripts test --env=node",
    "upload": "node -r dotenv/config ./scripts/upload.js ./build",
    "download": "node -r dotenv/config ./scripts/download.js ./download",
    "list": "node -r dotenv/config ./scripts/list.js",
    "push-static": "npm run build && npm run upload"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
